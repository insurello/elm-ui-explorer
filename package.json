{
  "private": true,
  "scripts": {
    "test": "elm-format --validate src tests && elm-test",
    "start": "cd example && elm-app start",
    "build": "cd example && elm-app build"
  },
  "dependencies": {
    "create-elm-app": "^4.2.4",
    "elm": "0.19.1-3",
    "elm-format": "0.8.2",
    "elm-test": "0.19.1"
  },
  "devDependencies": {
    "minimist": ">=1.2.2"
  }
}
